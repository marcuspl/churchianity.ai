% ── Fonts (LuaLaTeX) ──
\usepackage{fontspec}
\setmainfont{Latin Modern Roman}
\usepackage{newunicodechar}
\newunicodechar{≠}{\ensuremath{\neq}}
\setsansfont{Fira Sans}
\setmonofont{Fira Mono}

% ── Page Geometry (6×9 trade paperback) ──
\usepackage[
  paperwidth=6in,
  paperheight=9in,
  inner=0.85in,
  outer=0.65in,
  top=0.75in,
  bottom=0.85in,
  footskip=0.4in
]{geometry}

% ── Typography ──
\usepackage{microtype}
\usepackage{setspace}
\onehalfspacing

% ── Running Headers ──
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\fancyhead[LE]{\small\itshape Churchianity.ai}
\fancyhead[RO]{\small\itshape\leftmark}
\fancyfoot[C]{\thepage}
\renewcommand{\headrulewidth}{0.4pt}
\fancypagestyle{plain}{%
  \fancyhf{}
  \fancyfoot[C]{\thepage}
  \renewcommand{\headrulewidth}{0pt}
}

% ── Chapter Styling ──
\usepackage{titlesec}
\titleformat{\chapter}[display]
  {\normalfont\huge\bfseries\sffamily}
  {}
  {0pt}
  {\thispagestyle{plain}}
\titlespacing*{\chapter}{0pt}{-20pt}{30pt}

\titleformat{\section}
  {\normalfont\Large\bfseries\sffamily}
  {\thesection}{1em}{}

\titleformat{\subsection}
  {\normalfont\large\bfseries\sffamily}
  {\thesubsection}{1em}{}

% ── Blockquotes (for Reviewer Notes / Alternative Perspectives) ──
\usepackage{tcolorbox}
\tcbuselibrary{breakable}
\newtcolorbox{reviewbox}{
  colback=black!3,
  colframe=black!25,
  boxrule=0pt,
  leftrule=3pt,
  arc=0pt,
  breakable,
  left=8pt,
  right=8pt,
  top=6pt,
  bottom=6pt,
  before skip=12pt,
  after skip=12pt
}

% ── Tables ──
\usepackage{longtable}
\usepackage{booktabs}
\usepackage{array}

% ── Links ──
\usepackage[hidelinks]{hyperref}

% ── Misc ──
\usepackage{graphicx}
\usepackage{ellipsis}

% ── Pandoc tightlist ──
\providecommand{\tightlist}{%
  \setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}

% ── Redefine blockquote environment for pandoc ──
\renewenvironment{quote}
  {\begin{reviewbox}}
  {\end{reviewbox}}
